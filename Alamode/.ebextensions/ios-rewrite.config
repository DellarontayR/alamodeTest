files:
    "/etc/httpd/conf.d/ssl_rewrite2.conf":
        mode: "000644"
        owner: root
        group: root
        content: |
            RewriteEngine On
             <If "'%{HTTP_HOST}' !~ /^www\./">
            RewriteRule (.*) http://www.%{HTTP_HOST}%{REQUEST_URI} [R,L]
            </If>