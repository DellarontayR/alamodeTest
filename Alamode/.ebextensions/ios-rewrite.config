files:
    "/etc/httpd/conf.d/ios-rewrite.conf":
        mode: "000644"
        owner: root
        group: root
        content: |
            RewriteEngine On
            <If "'%{HTTP_HOST}' !~ /^www\./">
            RewriteRule (.*) https://www.%{HTTP_HOST}%{REQUEST_URI} [R,L]
            </If>